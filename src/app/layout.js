export const metadata = {
  title: 'Math-a-matik',
  description: 'Math Quiz',
}

export default function RootLayout({ children }) {
 return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
